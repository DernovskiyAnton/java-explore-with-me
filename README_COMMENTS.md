# Explore With Me - Feature: Комментарии к событиям

## 📋 Описание функциональности

Реализована возможность добавления комментариев к событиям. Пользователи могут:
- Комментировать опубликованные события
- Редактировать свои комментарии
- Удалять свои комментарии
- Просматривать комментарии к событиям

Администраторы могут удалять любые комментарии (модерация).

---

## 🚀 Установка

### 1. Переключиться на ветку feature/comments

```bash
git checkout feature/comments
```

### 2. Обновить schema.sql

Заменить файл `ewm-main-service/src/main/resources/schema.sql` на новый (с таблицей `comments`).

### 3. Добавить модуль comment в проект

Создать структуру:

```
ewm-main-service/src/main/java/ru/practicum/ewm/
└── comment/
    ├── model/
    │   └── Comment.java
    ├── dto/
    │   ├── CommentDto.java
    │   ├── NewCommentDto.java
    │   └── UpdateCommentDto.java
    ├── repository/
    │   └── CommentRepository.java
    ├── mapper/
    │   └── CommentMapper.java
    ├── service/
    │   └── CommentService.java
    └── controller/
        ├── PrivateCommentController.java
        ├── PublicCommentController.java
        └── AdminCommentController.java
```

### 4. Собрать проект

```bash
mvn clean package
```

### 5. Запустить в Docker

```bash
docker-compose up -d --build
```

### 6. Проверить работоспособность

Импортировать в Postman коллекцию `postman/feature.json` и запустить тесты.

---

## 📡 API Endpoints

### Private API (для пользователей)

#### POST /users/{userId}/events/{eventId}/comments
**Описание**: Добавить комментарий к событию

**Request Body**:
```json
{
  "text": "Отличное мероприятие! Очень понравилось."
}
```

**Response (201 Created)**:
```json
{
  "id": 1,
  "text": "Отличное мероприятие! Очень понравилось.",
  "eventId": 1,
  "authorName": "Иван Петров",
  "authorId": 1,
  "created": "2024-12-08 15:30:45",
  "edited": null
}
```

---

#### PATCH /users/{userId}/comments/{commentId}
**Описание**: Редактировать свой комментарий

**Request Body**:
```json
{
  "text": "Обновленный комментарий - еще лучше!"
}
```

**Response (200 OK)**:
```json
{
  "id": 1,
  "text": "Обновленный комментарий - еще лучше!",
  "eventId": 1,
  "authorName": "Иван Петров",
  "authorId": 1,
  "created": "2024-12-08 15:30:45",
  "edited": "2024-12-08 16:15:20"
}
```

---

#### DELETE /users/{userId}/comments/{commentId}
**Описание**: Удалить свой комментарий

**Response**: 204 No Content

---

### Public API (публичный доступ)

#### GET /events/{eventId}/comments?from=0&size=10
**Описание**: Получить комментарии к событию

**Query Parameters**:
- `from` (integer, default=0) - количество элементов для пропуска
- `size` (integer, default=10) - размер страницы

**Response (200 OK)**:
```json
[
  {
    "id": 1,
    "text": "Отличное мероприятие!",
    "eventId": 1,
    "authorName": "Иван Петров",
    "authorId": 1,
    "created": "2024-12-08 15:30:45",
    "edited": null
  },
  {
    "id": 2,
    "text": "Согласен, очень интересно было!",
    "eventId": 1,
    "authorName": "Мария Сидорова",
    "authorId": 2,
    "created": "2024-12-08 16:00:12",
    "edited": null
  }
]
```

---

### Admin API (для администратора)

#### DELETE /admin/comments/{commentId}
**Описание**: Удалить любой комментарий (модерация)

**Response**: 204 No Content

---

## 🔒 Бизнес-правила

1. ✅ Комментировать могут только **авторизованные пользователи**
2. ✅ Можно комментировать только **опубликованные события** (state = PUBLISHED)
3. ✅ Редактировать/удалять можно только **свои комментарии**
4. ✅ Администратор может **удалить любой комментарий**
5. ✅ Текст комментария: **от 1 до 5000 символов**
6. ✅ При редактировании автоматически проставляется поле `edited`
7. ✅ Комментарии отсортированы по дате создания (новые первые)

---

## ❌ Возможные ошибки

| HTTP код | Сценарий | Сообщение |
|----------|----------|-----------|
| 400 | Пустой текст комментария | `Comment text cannot be blank` |
| 400 | Текст > 5000 символов | `Comment text must be between 1 and 5000 characters` |
| 404 | Пользователь не найден | `User with id=X was not found` |
| 404 | Событие не найдено | `Event with id=X was not found` |
| 404 | Комментарий не найден | `Comment with id=X was not found` |
| 404 | Нет прав на редактирование | `Comment with id=X was not found or you are not the author` |
| 409 | Событие не опубликовано | `Cannot comment on unpublished event` |

---

## 🗄️ Структура базы данных

### Таблица `comments`

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | BIGSERIAL | Уникальный идентификатор |
| `text` | VARCHAR(5000) | Текст комментария |
| `event_id` | BIGINT | Ссылка на событие |
| `author_id` | BIGINT | Ссылка на автора |
| `created` | TIMESTAMP | Дата создания |
| `edited` | TIMESTAMP | Дата редактирования (nullable) |

**Индексы**:
- `idx_comments_event` - по событию
- `idx_comments_author` - по автору
- `idx_comments_created` - по дате создания

**Каскадное удаление**:
- При удалении события удаляются все его комментарии
- При удалении пользователя удаляются все его комментарии

---

## 🧪 Тестирование

### Автоматические тесты (Postman)

Импортировать коллекцию `postman/feature.json` и запустить.

**Покрытие**:
- ✅ Создание комментария (201)
- ✅ Валидация пустого текста (400)
- ✅ Получение комментариев (200)
- ✅ Редактирование (200)
- ✅ Попытка редактировать чужой (404)
- ✅ Удаление админом (204)
- ✅ Удаление пользователем (204)
- ✅ Удаление несуществующего (404)

### Ручное тестирование

```bash
# 1. Создать пользователя
curl -X POST http://localhost:8080/admin/users \
  -H "Content-Type: application/json" \
  -d '{"name":"Test User","email":"test@mail.com"}'

# 2. Создать и опубликовать событие (через Postman или curl)

# 3. Добавить комментарий
curl -X POST http://localhost:8080/users/1/events/1/comments \
  -H "Content-Type: application/json" \
  -d '{"text":"Отличное мероприятие!"}'

# 4. Получить комментарии
curl http://localhost:8080/events/1/comments?from=0&size=10

# 5. Редактировать комментарий
curl -X PATCH http://localhost:8080/users/1/comments/1 \
  -H "Content-Type: application/json" \
  -d '{"text":"Обновленный комментарий"}'

# 6. Удалить комментарий (админ)
curl -X DELETE http://localhost:8080/admin/comments/1
```

---

## 📊 Статистика реализации

- **Новых классов**: 8
- **Эндпоинтов**: 5
- **Строк кода**: ~500
- **Покрытие тестами**: 8 сценариев
- **Время реализации**: ~2-3 часа

---

## 📝 Список файлов

### Java классы
```
ewm-main-service/src/main/java/ru/practicum/ewm/comment/
├── model/Comment.java                      (Entity)
├── dto/CommentDto.java                     (Response DTO)
├── dto/NewCommentDto.java                  (Create DTO)
├── dto/UpdateCommentDto.java               (Update DTO)
├── repository/CommentRepository.java       (JPA Repository)
├── mapper/CommentMapper.java               (Mapper)
├── service/CommentService.java             (Business Logic)
└── controller/
    ├── PrivateCommentController.java       (User API)
    ├── PublicCommentController.java        (Public API)
    └── AdminCommentController.java         (Admin API)
```

### SQL миграция
```
ewm-main-service/src/main/resources/schema.sql  (обновлен)
```

### Тесты
```
postman/feature.json                        (Postman коллекция)
```

---

## 🎯 Соответствие требованиям Этапа 3

✅ **Ветка**: `feature/comments`  
✅ **Функциональность**: Полностью реализована  
✅ **Postman тесты**: Базовые тесты всех эндпоинтов  
✅ **Совместимость**: Не ломает существующий функционал  
✅ **Документация**: README с API и инструкциями

---

## 🔗 Pull Request

После завершения ревью:
1. Обновить README.md со ссылкой на Pull Request
2. Создать PR из `feature/comments` → `main`
3. Скачать ZIP из ветки `feature/comments`
4. После одобрения - Merge PR

---

## 👨‍💻 Автор

Дипломный проект Яндекс Практикум - Java Backend Developer  
Этап 3: Дополнительная функциональность "Комментарии к событиям"

